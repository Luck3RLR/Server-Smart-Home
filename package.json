{
  "name": "smart-home-api",
  "version": "1.0.0",
  "main": "api/server.js",
  "scripts": {
    "start": "node api/server.js",
    "dev": "vercel dev",
    "clean": "rm -rf node_modules package-lock.json .vercel",
    "reinstall": "npm run clean && npm install"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "overrides": {
    "glob": "^9.0.0",
    "rimraf": "^4.0.0",
    "lru-cache": "^7.0.0"
  }
}
